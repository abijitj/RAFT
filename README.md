# RAFT
An implementation of the RAFT Consensus protocol targeted towards embedded applications. 

Run `rustup update stable` and `rustup override set stable` in order to have rustc version >=1.89 for the redb@4.1.0 package to work.